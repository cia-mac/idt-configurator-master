# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
dist/

# macOS metadata
.DS_Store

# Local Claude Code config and telemetry (per-machine, not shared)
.claude/hook-log.json
.claude/settings.local.json
